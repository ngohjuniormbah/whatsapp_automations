# Later phases add: pydantic-ai (agent, Phase 3), apscheduler (Phase 5).
# Nothing beyond the agreed stack is pulled in without sign-off.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlmodel>=0.0.22
asyncpg>=0.29           # async Postgres driver for SQLAlchemy
greenlet>=3.0           # required by SQLAlchemy's async engine
pydantic-settings>=2.4,<3.0
twilio>=9.0,<10.0       # Phase 2: X-Twilio-Signature validation + TwiML

# Form bodies are parsed manually (urllib) so python-multipart is NOT
# needed for the Twilio webhook.
